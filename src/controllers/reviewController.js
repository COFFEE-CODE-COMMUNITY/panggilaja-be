const addReview = async (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};

const getServiceReview = async (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};

const getSellerReview = async (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};

export default { addReview, getServiceReview, getSellerReview };
